# /lms-review

Ти починаєш нову сесію роботи над LMS "Скарбниця". Виконай наступні кроки **перед тим як відповідати на будь-яке завдання**:

## 1. Прочитай контекст проєкту

- Прочитай `CLAUDE.md` — архітектура, конвенції, gotchas
- Прочитай `C:\Users\Boss\.claude\projects\d--lms\memory\MEMORY.md` — індекс пам'яті
- Прочитай файли пам'яті які здаються релевантними до поточної сесії (feedback, project, user)

## 2. Перевір поточний стан репозиторію

Виконай:
```
git log --oneline -10
git status
```

## 3. Сформуй короткий звіт для користувача

Виведи у форматі:

---
**Останні зміни** (з git log):
- [перелік останніх комітів — що змінювалось]

**Поточний стан** (git status):
- [змінені/нові файли якщо є, або "чисто"]

**Остання міграція БД:** v[N] — [що вона робила]

**Важливі нагадування** (з memory/feedback):
- [ключові правила поведінки які треба памʼятати в цій сесії]

**Готовий до роботи.** Що робимо?
---

## Правила виконання

- Не питай дозволу — просто виконай і виведи звіт
- Звіт має бути коротким (не більше 20 рядків)
- Після звіту чекай на завдання від користувача
