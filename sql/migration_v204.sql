-- v204: розділи для тестів (Контент → Тести) — групування списку тестів,
-- той самий підхід, що вже є для сторінок (custom_pages.section,
-- migration_v191): звичайне вільне текстове поле, без окремої таблиці —
-- "розділ" це просто значення, яке повторюється між тестами, підказки
-- автодоповнення будуються на льоту з унікальних значень.

ALTER TABLE public.tests ADD COLUMN IF NOT EXISTS section text;
